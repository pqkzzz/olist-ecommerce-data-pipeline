# Prefer DB_URL or provide individual values below.
# For docker-compose, use the postgres service host and container port.
DB_URL=postgresql://USER:PASSWORD@postgres:5432/DBNAME

DB_HOST=postgres
DB_USER=USER
DB_PASSWORD=PASSWORD
DB_PORT=5432
DB_NAME=DBNAME
DB_SCHEMA=public
DB_THREADS=4
